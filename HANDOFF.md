# Передача сесії: Data Exchange (`atm`)

Написано локальним Claude із сесії в теці `Projects\adealer` 18.08.2026, коли
роботу над ATM вирішено вести окремим вікном VS Code у **`C:\Users\chukhin\Projects\atm`**.

Пам'ять Claude прив'язана до теки проєкту, тож нова сесія тут стартує з чистою
пам'яттю. Цей файл — заміна: усе, що варто знати, зібрано нижче. Глобальний
`~/.claude/CLAUDE.md` підвантажується автоматично й діє як завжди.

---

## 1. Що це за продукт

**Data Exchange** — обмін документами між Odoo і зовнішньою обліковою системою
(1С, BAS) через JSON-файли в спільній теці. Другий публічно опублікований
додаток Сергія. 41 завантаження станом на 09.08.2026.

| | |
|---|---|
| Репозиторій | `rastafara66/atm`, **публічний** |
| Тека тут | `C:\Users\chukhin\Projects\atm` (модуль у підтеці `atm/`) |
| Гілки | `16.0`, `17.0`, `18.0`, `19.0` — по одній на серію Odoo |
| Ліцензія | LGPL-3, ціна 0 |
| Автор у маніфесті | `chukhin` (єдиний для всіх додатків — під ним шукають) |

### 🔴 Технічну назву `atm` не міняти

Сторінка додатка в магазині й лічильник завантажень тримаються саме на
технічній назві. Перейменування теки = новий додаток з нуля, з нульовим
лічильником. Відображувану назву («Data Exchange») міняти можна вільно —
стара «ATM — Automated Transaction Mechanics» читалась у ЄС/США як «банкомат»,
тому її й замінили.

### Що вміє

Обмін 10 сутностей окремими файлами: контрагенти, номенклатура, замовлення
клієнтів і постачальникам, рахунки клієнтам і постачальників, повернення в
обидва боки, оплати від клієнтів і постачальникам. Зіставлення записів за
`atm_external_ref` (міксин `atm.external.ref.mixin`), журнал обмінів,
перемикачі по сутностях, два крони (обидва вимкнені після встановлення),
українська локалізація `i18n/uk.po`.

## 2. Стан гілок на 19.08.2026

| Гілка | Версія | Примітка |
|---|---|---|
| `19.0` | **2.2.0** | усі чотири серії вирівняно |
| `18.0` | **2.2.0** | |
| `17.0` | **2.2.0** | + виправлено встановлення (`<tree>`) у 2.1.0 |
| `16.0` | **2.2.0** | + `<tree>` і `attrs` |

Що ввійшло: **2.1.0** — ПДВ по ставках і крос-посилання на ЄДР (рознесено на всі
серії рішенням Сергія 18.08); **2.1.1** — скріншоти двома мовами; **2.2.0** —
автоматичні звіти про помилки (розділ 4a).

🔴 **Після кожного пуша — руками `/apps/dashboard/repos` → Scan**, по одному
запису на серію (їх чотири). Автоскан вимкнено з 09.08.2026.

🔴 **Звіряти хеші, а не вірити «запушено».** 19.08 три гілки долетіли, а `19.0`
ні — мережа обірвала пуш, і сторінка чесно показувала стару версію. Виглядало
як «магазин не оновлюється», а сканувати просто не було чого. Перевірка:
`git ls-remote origin refs/heads/<гілка>` проти `git rev-parse <гілка>`.

## 3. Правила магазину — здобуті на цьому ж модулі, кожне коштувало циклу

1. **Сторінка оновлюється ЛИШЕ коли змінюється `version` у маніфесті.** Скан
   без зміни версії не робить нічого — можна тиснути хоч десять разів. Саме на
   цьому 08.08 згорів цілий вечір.
2. 🔴 **Підняти версію і дописати changelog — це ОДНА дія, не дві.** Двічі
   траплялось: сторінка чесно оновлювалась, але розділ, за яким Сергій
   перевіряє результат, лишався слово в слово тим самим.
3. **`index.html` має бути чистим ASCII.** Магазин віддає опис без указання
   кодування, тож кирилиця йде крокозябрами. Уся не-латиниця — HTML-entity
   (`&#1054;` тощо). Перевірка: `sum(1 for c in text if ord(c) > 127)` має бути 0.
4. **`images[0]` — картка додатка** в списку, решта йде в галерею. Скріншот у
   мініатюрі читається як сіра пляма, тому першим має стояти банер.
5. **Гілка мусить називатись рівно як серія Odoo** (`19.0`, не `main`).
   URL репозиторію в магазині — в SSH-схемі з гілкою після `#`:
   `ssh://git@github.com/rastafara66/atm#19.0`.
6. **Автосканування Сергій вимкнув** 09.08.2026 — після того, як скан сам
   виставив у продаж непідготовлений модуль в іншому репо. Отже після пуша
   магазин **не побачить змін сам**: треба зайти на `/apps/dashboard/repos` і
   натиснути **Scan**. По одному репо, з паузами.
7. **`apps.odoo.com` рахує запити з однієї IP** і показує «Keep calm and
   breathe deeply». Це не помилка й не бан — почекати кілька хвилин. Перевірено
   09.08: на машині Сергія жодного фонового процесу до магазину немає, це
   просто наплив живих запитів.
8. **Приватним репо потрібен колаборант `online-odoo`** з правом Read (це НЕ
   deploy-key). Для `atm` не потрібно — репо публічне.

## 4. Пастки в коді ATM — усі перевірені, кожна коштувала окремого фіксу

* **`res.config.settings` не приймає `Date` з `config_parameter`** — екран
  налаштувань падає з «Oops! Something went wrong». Булеві там теж не годяться:
  Odoo видаляє параметр, коли зберігається `False`, і «вимкнено користувачем»
  стає не відрізнити від «ніколи не налаштовували» — вимкнена сутність
  поверталась увімкненою. Читати й писати вручну в `get_values` / `set_values`,
  зберігаючи рядки `'True'` / `'False'`.
* **Поле номера оплати різне за версіями:** `memo` у 18/19, `ref` у 16/17.
  Це два місця: `_payment_spec` у `entities.py` (`'memo': 'ref'`) і
  `SEQUENCE_OWNED_MODELS` у `atm_exchange.py`. 18.08.2026 друге виправили, а
  перше майже пропустили.
* 🔴 **Вигляд списку: `<tree>` у 16/17, `<list>` у 18/19.** Через це модуль
  **не встановлювався** на 16.0 і 17.0 з 08.08 до 18.08.2026 — коміт `a81c550`
  переніс вигляд журналу з 19.0 цілим файлом. Три місця в одному файлі:
  `<list>`, `</list>`, `view_mode>list,form`.
* 🔴 **Не копіювати файли між гілками цілком.** Обидві пастки вище — одна й та
  сама помилка. Переносити скриптом, який після правок вимагає
  `git diff <гілка> --numstat` по `entities.py` з **нулем видалених рядків**
  (зміни завжди лише додають). Далі — ставити модуль у чисту базу кожної серії,
  не лише компілювати: саме цього бракувало трьом релізам 2.0.x.
* **Податки рядка звуться по-різному:** `tax_id` (продаж) і `taxes_id`
  (закупівля) у 16–18, `tax_ids` для обох у 19. Рушій приймає список
  альтернатив (`LINE_TAX_FIELD`), тож нових розгалужень не потрібно.
* **Одиниця виміру в рядку замовлення:** `product_uom` у 16–18,
  `product_uom_id` у 19. А в рядку проведення — `product_uom_id` завжди.
* **Умовна видимість у виглядах:** `attrs="{'invisible': [(...)]}"` у 16.0,
  звичайний вираз (`invisible="state == 'sent'"`) з 17.0.
* **SQL-обмеження:** `models.Constraint` існує **лише в 19.0**. Писати
  `_sql_constraints` — його приймають усі чотири серії, і код не розгалужується.
* **Кирилиця в скриптах для `odoo-bin shell`** — лише `\uXXXX`. Через stdin на
  Windows пряма кирилиця приходить мохібейком і **тихо лягає в базу**: так
  зіпсувалися назви податкових груп, і помітилось аж на скріншоті для магазину.
  Діагностувати кодами символів (`'%04X' % ord(c)`), не оком — консоль бреше.
* **Дублікат `msgid` у `uk.po`** тихо блокує **весь** доданий блок перекладів —
  ні помилки, ні попередження. Перевіряти `Counter` по `msgid`, оновлювати з
  `--i18n-overwrite`, звіряти результат кодами символів.
* ⚠️ **`.pot` у модулі немає — і не заводити неповний.** Odoo зливає `uk.po` з
  шаблоном і викидає все, чого в шаблоні нема; зараз переклад цілий саме тому,
  що шаблону нема.
* **`--test-tags=/atm` у Git Bash** треба запускати з `MSYS_NO_PATHCONV=1`,
  інакше `/atm` стає шляхом Windows і тести мовчки не виконуються.
* **У демо-даних Odoo нуль платежів** — тестувати оплати можна лише на
  створених вручну (`scratchpad/seed_payments.py` свого часу це робив).
* **Round-trip у порожню базу не ловить `skipped`.** Перевіряти імпорт назад у
  ту саму базу й звіряти `created + updated + skipped + failed = записів у файлі`.
* **Іконка й банер малюються кодом:** `tools/make_icons.py` (Pillow, рендер у
  4× із подальшим зменшенням — інакше краї зубчасті).
* **Стиснення скріншотів — квантуванням у 256 кольорів**, не зменшенням
  розміру: LANCZOS розмиває різкий текст, і PNG стає *більшим* (перевірено:
  122 КБ → 224 КБ після «оптимізації», і лише квантування дало 80 КБ).

## 4a. Звіти про помилки (2.2.0)

Правило лінійки: **кожен додаток має мати changelog і звіт про помилки.**
В ATM зроблено 19.08.2026 за зразком `adealer` (не Bank Sync — там є банківські
провайдери, яких тут нема). **Копія, а не залежність:** модулі лінійки не
залежать один від одного, інакше їх не можна продавати окремо.

Приймач **спільний і вже стоїть на VPS** — `https://yellow.in.ua/odoo-report`,
той самий upstream, що й `/bank-sync/report`. Новий не піднімати: модулі
розрізняються полем `module`. Наскрізний прогін 19.08 — прийняв.

🔴 **Текст помилки не відправляється ніколи.** Летить клас винятку, рядки коду
зі шляхами, обрізаними до кореня модуля, версії, HTTP-статус, випадковий
`install_id` і коментар користувача. Безпека структурна: через поле, у якому
буває лише `KeyError`, злити ім'я клієнта неможливо.

Дві відмінності ATM від зразків:

* **Декоратор не працює.** Рушій сам ловить свої винятки й пише в журнал
  обміну — назовні нічого не вилітає. Тому `_capture` викликається зсередини
  наявних `except`-блоків через `AtmExchange._report_crash(operation)`.
* **`AtmDataError`** (`models/atm_exceptions.py`) — «інша система прислала
  товар, якого тут нема». Такі збої приходять цілими файлами й уже рахуються
  як `failed`; звітувати про них — поховати справжній баг. Внесений до
  `EXPECTED_EXCEPTIONS` поряд із `UserError`. Перевірено живцем: справжній баг
  в експорті → 1 звіт, 40 нерозв'язних записів при імпорті → 0 звітів.

🔴 **Не додавати текст помилки у звіт.** Тест
`test_payload_carries_no_business_data` навмисно кидає помилку з іменем клієнта,
номером рахунка, адресою й сумою. Якщо колись додати текст — впаде саме він.
Не прибирати. Це перші тести в модулі взагалі (10 штук).

## 5. Середовища

| Що | Де |
|---|---|
| Odoo 16 / 17 / 18 | `D:\odoo16\server`, `D:\odoo17\server`, `D:\odoo18\server` (для 16 і 17 потрібен `ODOO_RC`) |
| Odoo 19 | `C:\odoo19`, python у `C:\odoo19\.venv\Scripts\python.exe` |
| Демо-база | `atm_demo` (існує), стенд піднімався на порту **8075** |
| PostgreSQL | служба `postgresql-odoo19`, `127.0.0.1:5432`, user `odoo` / пароль `odoo` (див. `C:\odoo19\odoo.conf`) |

🔴 **Пастка з подвійною копією.** `C:\odoo19\my_addons\atm` — це **стара копія
модуля версії 19.0.1.0 від березня 2026**: не git-репозиторій, не junction, з
`Projects\atm` не пов'язана ніяк. Локальна Odoo 19 вантажить `my_addons`, тобто
бачить **старий ATM 1.x**, а не нинішній 2.0.4. Якщо правки «не діють» — це
перше, що перевіряти. Рішення (з Сергієм не узгоджене): заархівувати ту теку й
поставити на її місце junction на `Projects\atm\atm`.

## 6. Що лишилось зробити

* ✅ **ПДВ по ставках — зроблено 18.08.2026** (2.1.0, усі чотири серії).
  Документи везуть блок `tax_summary`: по рядку на ставку з базою, сумою
  податку й підсумком, плюс група для рядків без ПДВ. Цифри — агрегація того,
  що Odoo вже порахувала (`price_subtotal` / `price_total`), тож сходяться з
  `amount_untaxed` і `amount_tax` до копійки. Формат описано в `atm/README.md`,
  розділ «Taxes». Це знімає передумову для конектора М.Е.Doc.
* **Скріншоти двома мовами — незавершено.** Рішення 09.08 для 3A-dealer,
  поширене сюди: кожен екран двічі (`uk_UA` / `en_US`), англійські йдуть у
  галерею магазину, обидві мови — на сторінку опису. Зроблено три українські
  (`screenshot_settings_uk.png`, `screenshot_entities_uk.png`,
  `screenshot_log_uk.png`). Бракує рахунка й JSON українською та всіх
  англійських. Після цього — оновити `images` у маніфесті й вставити кадри в
  `index.html`.
* ✅ **2.0.4 на решту гілок — зроблено**, разом із ПДВ під однією версією.
* ✅ **Звіти про помилки — зроблено 19.08.2026** (2.2.0, усі серії), див. 4a.
* **Стенд для скріншотів.** База `atm_demo` підготовлена: українська мова,
  ставки ПДВ 20% і 7% (назви перекладні, тому в en-інтерфейсі «VAT 20%»),
  власні податкові групи, два рахунки в UAH на вигаданого `Promin LLC`, тека
  обміну `D:\ATM_Export`. Підіймати командою з розділу 5, порт 8075.
* **`atm_1c` як товар** — узагальнити напрацювання з `tools/1c_import` (лежить
  у репо `3A`, тека `Projects\adealer`) до рівня, коли воно ставиться в чужу
  базу без правки коду.
* **Платна версія.** LGPL-3 опубліковане **не відкликається**: код уже
  публічний. Платним може стати лише **новий** модуль або **наступна мажорна**
  версія. Ніколи не обіцяти «зробимо наявні платними». OPL-1 може залежати від
  LGPL-3, але не від GPL-3.

## 7. Місце в лінійці

Data Exchange — один із чотирьох продуктів української лінійки Odoo «перехід з
1С». Повний план: **`C:\Users\chukhin\Projects\adealer\plan_ua_suite.md`**.

| Продукт | Код | Живий інстанс |
|---|---|---|
| **Data Exchange** (`atm`) | `Projects\atm` | локальні тест-Odoo 16–19 |
| **3A-dealer** | `Projects\adealer-store` (код), `Projects\adealer` (журнали, плани, tools) | локальна Odoo `:8070` |
| **3A-dealer Pro** | `Projects\adealer-pro` (приватне) | там само |
| **Bank Sync** | `Projects\odoo-bank-sync` | VPS `~/odoo-fop` «Рахівник», `10.0.0.1:8095` — **справжні дані**, скріншоти звідти не робити |
| **Рахівник** | репо `rakhivnyk` (локальної копії немає) | той самий `~/odoo-fop` |
| **Контрагенти з ЄДР** | `Projects\yellow-edr-connector` | бази `edr_demo`, `edr_clean` |

Ключове правило лінійки: **модулі не залежать один від одного**, лише від
штатних моделей Odoo (`res.partner`, `product.product`, `account.move`,
`account.payment`). Інакше їх не можна продавати окремо. Спільний ключ
ідентичності — `atm_external_ref` із цього модуля; нових полів зовнішнього
ключа в інших модулях не заводити.

## 8. Як працює Сергій

* **Відповідати українською.** Пояснювати простою мовою з побутовою аналогією,
  покроково — не скоромовкою термінів.
* **Commit + push після кожного завершеного шматка**, не чекаючи прохання.
* **Рутинні локальні операції робити самому** (дроп тестових баз, конфіги,
  рестарт dev-Odoo). Питати лише про ризикове, зовнішнє й незворотне.
* **Ніколи не скидати паролі Odoo** без явного прохання. Секрети — лише в
  gitignored файлах.
* **Клієнтів не називати ніде в публічному** (Ford, Автоімпульс). А от
  **1С / BAS / М.Е.Doc згадувати можна й треба** — це те, що люди шукають.
* **М.Е.Doc як завдання відкладено** рішенням 09.08.2026 — не планувати, поки
  не буде окремого слова.
* **Скріншоти** Сергій кладе в `C:\Users\chukhin\Pictures\Screenshots\` —
  брати найсвіжіші за часом, перейменовувати й копіювати самому, шлях не
  перепитувати. Перед публікацією дивитись, чи немає особистих даних (IBAN,
  номери карток, залишки, ПІБ): якщо є — не блюрити, а робити знімок з
  **демо-даних**.
* **Попереджати про будь-які платні дії** від €0.01.
